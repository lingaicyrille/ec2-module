#local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*
*.terraform.lock.hcl

#Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to 
# password, private keys, and others secrets.
# control as they are data points which are potent
# to change depending on the environment.
*.tfvars.json

#ignore override files as they are usually used to 
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json
# include override files you do wish to add to

.terraformrc
terraform.rc
